if(env.OS_Info == "windows") {
    script {
        // to be contributed: individual postprocessing (windows)
    }
} else {
    script {
        // to be contributed: individual postprocessing (not windows)
    }
}
